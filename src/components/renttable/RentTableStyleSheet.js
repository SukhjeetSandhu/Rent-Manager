const rentTableStyleSheet = {
    container: {
        height: `calc(100vh - ${36}px)`,
        boxSizing: 'border-box'
    },
    header: {
        fontSize: '1.5rem'
    },
    row: { cursor: 'pointer' },
    overflowAuto: { overflow: 'auto' }
}

export default rentTableStyleSheet;